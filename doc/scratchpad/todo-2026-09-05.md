# TODO discovery — 2026-09-05

**Working note. Decides nothing.** A snapshot of what the documents already say about each other,
regenerated by re-running the `todo-discovery` skill, and cleared when the goal lands. No priority
and no recommendation: ordering is the owner's.

---

## D-1 · spec-app.md cites `scope.md` for content the scope no longer holds

**Kind.** Dangling reference.

**Claim.** Five sentences in `spec-app.md` defer to `scope.md` for decisions, and one quotes it
verbatim. `scope.md` now holds no goal, so none of them resolves to anything.

**Evidence.** `doc/spec-app.md:8` (*"What is in version one at all is scope.md"*), `:11` (*"scope.md
puts all of them out"*), `:22` (*"scope.md leaves a player exactly one thing to do"*), `:49`
(*"scope.md puts sound out of version one"*), `:61` (*"scope.md names it"*), `:96` (quotes *"the
keyboard on a desktop-sized screen, and touch on a small one"*) · `doc/scope.md:26`, which is the
whole of what the document now says.

**Done when.** Each sentence states the decision as spec-app's own, or attributes it to the
specification that owns it — which `doc/scope.md:20` already requires, since a specification
outranks the scope the moment it exists.

## D-2 · spec-domain.md cites `scope.md` for content the scope no longer holds

**Kind.** Dangling reference.

**Claim.** Three sentences defer to `scope.md` for what version one excludes.

**Evidence.** `doc/spec-domain.md:8`, `:11` (*"scope.md puts every one of them out"*), `:180`
(*"scope.md puts them out of version one"*) · `doc/scope.md:26`.

**Done when.** Same edit as D-1, in `spec-domain.md`.

## D-3 · spec-style.md cites `scope.md`'s "In" list, which no longer exists

**Kind.** Dangling reference.

**Claim.** `spec-style.md` scopes itself to a named section of `scope.md` and cites it as the source
of the Tron decision. Neither the section nor the list is in the document.

**Evidence.** `doc/spec-style.md:5` (*"nothing in doc/scope.md yet to attach to"*), `:12` (*"Scoped
to what doc/scope.md's 'In' list needs"*), `:20` (*"Named in doc/scope.md's first-version 'In'
list"*), `:78`, `:81`, `:88` · `doc/scope.md:26`.

**Done when.** Same edit as D-1, in `spec-style.md`. `:20` additionally needs a source for the Tron
decision that still exists — `doc/brainstorm/2026-09-02.md` is already cited beside it.

## D-4 · The authored level's own comment cites `scope.md`

**Kind.** Dangling reference.

**Claim.** `first.ts` names `doc/scope.md` as what makes it version one's one authored level.

**Evidence.** `src/levels/first.ts:2` · `doc/scope.md:26`.

**Done when.** The comment cites the specification that owns the level, or drops the citation.

## D-5 · The `app` skill tests activities against a scope that may hold no goal

**Kind.** Stale claim.

**Claim.** The skill instructs testing every activity against `doc/scope.md` and calling an activity
the scope puts out a finding. Between a goal being cleared and the next being set, there is nothing
there to test against, and the skill does not say what to do then.

**Evidence.** `.claude/skills/app/SKILL.md:45`, `:76` · `.claude/skills/scope/SKILL.md:22`, which
establishes that a scope holding no goal is a state the project is allowed to sit in.

**Done when.** The skill says what that test does when no goal is set.

## D-6 · The `style` skill cites guide-general for a sentence it no longer contains

**Kind.** Contradiction.

**Claim.** The skill tells the reader to run the dev server and screenshot the page *"per
guide-general.md"*. guide-general no longer carries that argument, and now carries close to its
opposite: the suite is the verification and driving the product by hand is not.

**Evidence.** `.claude/skills/style/SKILL.md:54-56` · `doc/guide-general.md:60-63`.

**Done when.** The style page is named as the exception it is — a page whose whole purpose is to be
looked at, which is a deliverable rather than a verification step — in whichever of the two documents
should carry that, and the citation points at something that exists.

## D-7 · spec-tech's A-2 records the level seam but not that it departs from guide-design

**Kind.** Routing gap.

**Claim.** `guide-design.md` requires a dev-only affordance to carry a gate so it never ships
enabled. The level seam added this goal ships enabled in the production build: the query parameter
works on the deployed page. guide-design's own escape is that spec-tech outranks it where the two
disagree, **provided the departure is recorded there in one line**. A-2 records the seam and its
limits, and says nothing about the rule it departs from.

**Evidence.** `doc/guide-design.md:89` (*"Dev-only affordances are built, and gated ... so they never
ship enabled"*) · `doc/spec-tech.md`, A-2 · `src/main.ts`, `chosenLevel`.

**Done when.** A-2 names the departure and why the gate the style page uses — being left out of the
build — cannot be used here, since the end-to-end suite tests the built page.

## D-8 · The `land` skill reads a section that does not exist when no goal is set

**Kind.** Dangling reference.

**Claim.** `land` tells the reader to read the scope's *Done means* on every landing. When the scope
holds no goal there is no such section.

**Evidence.** `.claude/skills/land/SKILL.md:69` · `doc/scope.md:26`.

**Done when.** The instruction says that a scope with no goal in it has nothing to check.

---

## Blocking

**D-7 is the only finding that describes shipped behaviour**; the rest are documents disagreeing.
Nothing here blocks anything else.

D-1 to D-4 have one cause — the scope being cleared on reaching its goal — and one shape of fix, so
they are cheaper taken together than separately.

## Examined and excluded

| Examined | Result |
|---|---|
| Every markdown link target and backticked path in every tracked `.md`, resolved against the tree | All resolve. No dangling paths |
| CLAUDE.md's active lesson list against spec-tech's chosen stack | Agree. `firebase.md` and `sveltekit.md` exist and are correctly absent, their technologies being unchosen |
| `setup-app-env.md`'s named artefacts — `.github/workflows/deploy.yml`, `scripts/verify-deployment.sh` | Both exist |
| CLAUDE.md's routing table against the documents in `doc/` | Every row resolves; every document exists |
| `grep -i 'todo\|fixme\|tbd'` | Nothing beyond this file and the task breakdown |
| `spec-app.md:47` and `spec-style.md:83` describing a `CLEARED` indication the renderer does not draw | **Excluded** — unbuilt implementation, and both specifications say in their own headers that they state the desired state and the code is what is wrong |
| Deployment existing while the reached goal put *being deployed* out of version one | **Excluded** — the goal is cleared, so there is no longer a document making the claim |
| The four specifications checked against each other | **Excluded** — the skill says this stays out of scope until the owner makes it a decision |
