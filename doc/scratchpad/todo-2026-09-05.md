# TODO discovery — 2026-09-05

**Working note. Decides nothing.** A snapshot of what the documents already say about each other,
regenerated by re-running the `todo-discovery` skill, and cleared when the goal lands.

**Second run of the day, superseding the first.** The first run's eight findings were all closed on
this branch before this run; superseding is what the skill asks for rather than editing the earlier
snapshot towards the truth. What each finding was and how it was closed is in the commit history,
one commit per finding, `D-1` through `D-8`.

---

## Findings

**None open.**

## What the re-run checked

| Checked | Result |
|---|---|
| `scope` appearing anywhere in `spec-app.md`, `spec-domain.md`, `spec-style.md` or `src/` | None. D-1 to D-4 closed |
| The `app` skill's activity test naming the empty-goal case | Present at `:77`, `:79`. D-5 closed |
| The `style` skill's citation of `guide-general.md` | Now cites it for a rule that exists, and names itself the exception. D-6 closed |
| `spec-tech.md` A-2 naming the guide-design rule it departs from | Present at `:94`. D-7 closed |
| The `land` skill on a scope holding no goal | Present at `:72`. D-8 closed |
| Every markdown link target and backticked path in every tracked `.md`, re-resolved against the tree | All resolve |
| Documents naming a *section* of another document — *Done means*, *Where each answer ends up* | One overstatement found and fixed in the same pass: the `scope` skill assumed a goal always carries an answers table, where `doc/scope.md:20-22` says only a goal holding answers does |

## Examined and excluded

| Examined | Reason |
|---|---|
| `spec-style.md` still carrying brainstorm provenance in its own voice — *"the owner's screenshot answer, given 'without hesitation'"*, *"answered 'no idea'"* | **Raised with the owner and not acted on.** It is the same family as the rule that a specification states its decisions as its own, but the source document is the brainstorm rather than the scope, and widening the rule is the owner's call |
| `spec-app.md:47` and `spec-style.md:83` describing a `CLEARED` indication the renderer does not draw | Unbuilt implementation, and both specifications state in their headers that they describe the desired state |
| The four specifications checked against each other | Out of this skill's scope until the owner makes it a decision |
| `grep -i 'todo\|fixme\|tbd'` | Nothing outside this directory |

## A caution for the next run

The reference resolver used here reports three false positives, and they are the resolver's limits
rather than findings: a `` `doc/lessons/<technology>.md` `` placeholder, which is a shape and not a
path, and two bare filenames cited from this directory, which resolve relative to the citing file.
