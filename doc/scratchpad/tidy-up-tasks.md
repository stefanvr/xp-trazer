# Tidy up — what is left

**Working note. Decides nothing.** A task is removed once it is implemented, so this file always
says what is left. Cleared when the goal lands.

| Task | Checkable as |
|---|---|
| **1 · A reached scope is cleared, and says what comes next** | `scope check` writes no goal text back: the marker replaces everything from the goal's heading down, and points at discovery and then `scope create`. `doc/scope.md` carries no finished goal, and its header, the `scope` skill and CLAUDE.md all agree on that |
| **2 · The e2e suite asserts a level being cleared** | A test level of one destructible brick placed where the launched ball hits it directly; the suite watches bricks reach zero without steering anything, and fails if clearing stops working |
| **3 · Run `todo-discovery`** | A dated snapshot in this directory, and every finding given a fate before the goal lands |

Task 1 reverses the shape landed in `be7090b`, on the owner's correction: a document still full of
a finished goal is what makes the next goal less obvious.
