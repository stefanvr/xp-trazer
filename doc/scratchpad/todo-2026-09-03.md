# TODO discovery — 2026-09-03

**Working note. Decides nothing, and is never cited as authority.** A snapshot of what the documents
already say about each other, regenerated by re-running the `todo-discovery` skill and cleared when
the goal it belongs to lands.

Every document in `doc/` and every `SKILL.md` was read end to end. Eight findings, all cited.

---

## D-1 · The scope says the stack is unchosen; it has been chosen

**Kind.** Contradiction.

**Evidence.** `doc/scope.md:53` — *"[spec-tech.md] chooses nothing today: no runtime, no framework, no
renderer, no test runner"*, under a heading calling that choice *"the goal in front of everything
else"*. `doc/spec-tech.md:12-19` records TypeScript, Node 24 LTS, Vite, Canvas 2D, Vitest, Playwright,
GitHub Pages and GitHub Actions. `doc/scope.md:80` — the same document's own table marks *"The stack,
once chosen"* as ✅.

**Done when.** `doc/scope.md`'s *The first goal inside it — choose the stack* section says what is now
true, or is removed because the table already records it.

## D-2 · setup-ai-env says spec-tech has chosen no CI

**Kind.** Contradiction.

**Evidence.** `doc/setup-ai-env.md:214` — *"If CI turns out to be GitHub Actions — spec-tech has chosen
no CI —"*. `doc/spec-tech.md:19` — `| CI | GitHub Actions |`.

**Done when.** SF-7's parenthetical states the choice instead of hedging it.

## D-3 · SF-8 describes the deployment check as something the project does not have yet

**Kind.** Stale claim.

**Evidence.** `doc/setup-ai-env.md:235` — *"it is the check to verify this project's deployment with,
once it has one"*. The project has one: `scripts/verify-deployment.sh`, run by
`.github/workflows/deploy.yml:86` and documented at `doc/setup-app-env.md:36`.

**Done when.** SF-8 points at the check that now exists.

## D-4 · SF-12 says this repository has no executable file, and names the one it now has

**Kind.** Stale claim.

**Evidence.** `doc/setup-ai-env.md:286` — *"Observed on `scripts/verify-deployment.sh`, the only
executable file of an earlier project on this machine — this repository has none yet"*. This
repository now tracks `scripts/verify-deployment.sh` at mode `100755`.

**Done when.** The sentence stops saying the file is absent here.

## D-5 · The session-start checklist hedges a Node version that is now pinned

**Kind.** Stale claim.

**Evidence.** `doc/setup-ai-env.md:386` — *"the version spec-tech pins, once it pins one"*.
`doc/spec-tech.md:13` pins Node 24 LTS, and `.nvmrc` carries it.

**Done when.** The checklist row names the pinned version.

## D-6 · The style skill puts its own page at the repository root

**Kind.** Stale claim.

**Evidence.** `.claude/skills/style/SKILL.md:45` — *"a second root-level HTML file"*. The file is
`dev/style.html`, and the same section names that path two lines earlier.

**Done when.** The sentence describes where the file actually is. *(The gate itself holds — see
exclusions.)*

## D-7 · The style skill forbids a second copy of a colour; four exist

**Kind.** Contradiction.

**Evidence.** `.claude/skills/style/SKILL.md:38-40` — *"Every color is a named constant in one module
— src/render/palette.ts — imported by both the real renderer and this page … there must never be a
second copy."* Colour literals outside that module: `src/style.css:3` (`#04070d`), `src/style.css:4`
(`#31e3ff`), `src/style.css:27` (`rgb(49 227 255 / 0.25)`), `dev/style.css:8-9` (`#000`, `#cdeaf2`).
`src/style.css:3` also differs from `doc/spec-style.md`'s playfield background `#05080d` by one digit,
so the page and the playfield are two near-blacks rather than one.

**Done when.** Either the rule says it governs playfield colours only, or those literals move into
`src/render/palette.ts`.

## D-8 · The discovery skill lists as possibly-absent two things that now exist

**Kind.** Stale claim.

**Evidence.** `.claude/skills/todo-discovery/SKILL.md:17` — *"Never report that the stack is unchosen,
that `spec-domain`, `spec-app` or `spec-style` are missing"*. `.claude/skills/todo-discovery/SKILL.md:63`
— *"Once `spec-domain`, `spec-app` and `spec-style` are written"*. `doc/spec-style.md` exists and
`doc/spec-tech.md:8-19` chooses the stack.

**Done when.** Both sentences name only `spec-domain` and `spec-app`.

---

## A fact about the set, not an ordering

**D-2 to D-5 are all in `doc/setup-ai-env.md`, and all four were made stale by the same event** — the
deployment goal landing. Nothing blocks anything else; every finding closes independently.

## Examined and excluded

- **`spec-domain.md` and `spec-app.md` do not exist**, and five markdown links point at them
  (`doc/scope.md`, `doc/spec-style.md`, `doc/spec-tech.md`). Excluded: a specification the sequence
  has not reached is the plan working, and this skill names it as the most tempting non-finding. The
  links are the absence wearing a dangling-reference costume.
- **The style page's gate.** `.claude/skills/style/SKILL.md:43-48` claims `dev/style.html` is left out
  of `vite build`. Verified by running the build: `dist/` contains `index.html` only. Holds.
- **`doc/setup-app-env.md`'s *What only a person can do*.** Both actions have since been taken, but
  the section is standing guidance for any environment, not a task list. Not stale.
- **O-1 in `doc/guide-override.md`.** A live override that names what would end it.
- **`core.fileMode` noise in a fresh clone.** SF-12 states it and the session-start checklist repairs
  it — written down and handled.
- **`style` and `todo-discovery` have no hook in CLAUDE.md** where `brainstorm`, `build` and `land`
  do. Considered as a routing gap and rejected: a skill is a routine, not a kind of decision, and
  both carry their own triggers in their descriptions.
- **Cosmetics.** `README.md:3` *"inspire"*; `doc/setup-ai-env.md:3` *"How to AI interacts"*.
- **CI's Node 20 deprecation warnings.** A platform notice about GitHub's own action runtime, not an
  inconsistency between this project's artefacts.
- **`grep -inE "\b(todo|fixme|tbd)\b"`** over every tracked file: seven hits, all of them the
  `todo-discovery` skill's own name or `land`'s prose about not keeping a todo list
  (`.claude/skills/land/SKILL.md:45,53`, `.claude/skills/todo-discovery/SKILL.md:2,6,22,79,86`). No
  marker anywhere in code or documents. As the skill predicts — the greppable half is the smallest
  half.
