# Open inconsistencies — 2026-09-03

**Working note. Decides nothing, and is not authority.** A snapshot from the `todo-discovery` skill,
superseding the run of 2026-09-02. Re-run the skill rather than editing this file.

**Scope.** Process and documents only: what they claim about each other, what they point at, and
whether every kind of decision has one owner. Missing specification content and unbuilt
implementation are out of scope by definition — see the skill.

**What was examined.** All fourteen tracked files, and every file reference in `doc/`,
`.claude/CLAUDE.md` and both skills tested for existence.

---

## D-1 · guide-design.md still says nothing owns the game, after CLAUDE.md gave it an owner

**Kind.** Stale claim.

**Evidence.** [guide-design.md:6-7](../guide-design.md#L6-L7) — *"What the game is to play is not
owned by any document yet, and does not belong here when it is."* Against
[CLAUDE.md](../../.claude/CLAUDE.md), whose routing table now sends *"What are the rules of the game
world?"* to `doc/spec-domain.md`.

**Why it survived the fix.** The routing was added in one document and the sentence it falsified sits
in another. This is the ordinary aftermath of a structural repair, and the reason the skill now says
to re-read whatever changed most recently.

**Done when.** guide-design.md points at `spec-domain.md` as the owner instead of reporting that
there is none — the *does not belong here* half is still correct and should stay.

---

## D-2 · "the deletion rule" is cited as though defined elsewhere, and nothing defines it

**Kind.** Dangling reference.

**Evidence.** [setup-ai-env.md:332](../setup-ai-env.md#L332) — working notes belong in
`doc/scratchpad/` *"where the deletion rule clears them when the goal lands."* No rule of that name
exists in any document. The trailing clause is the only statement of it there is.

**Done when.** Either the rule is written where it can be followed, or the phrase stops presenting
itself as a citation and simply states the rule inline.

**Note.** The directory half of this finding closed on 2026-09-02; `doc/scratchpad/` now exists.

---

## What blocks what

Nothing. Both findings are edits to a single document each, and neither depends on the other.

## Examined and excluded

- **The stack is unchosen; `spec-domain`, `spec-app` and `spec-style` do not exist; nothing is
  implemented.** Out of scope by definition. The bootstrap sequence in CLAUDE.md has not reached them,
  and CLAUDE.md's routing table states that explicitly, so the references to those three files are
  declared rather than dangling.
- **`scripts/verify-deployment.sh`, the CI smoke test, the Firebase deployment.** Named in
  setup-ai-env.md and absent here — no longer findings, since those passages now attribute themselves
  to earlier projects on this machine.
- **`core.fileMode` starts noisy in a fresh clone.** Stated in SF-12 with its remedy in the
  session-start checklist. Written down and handled.
- **`.gitignore` ends without a newline.** Cosmetic.

## Superseded

The 2026-09-02 run listed six findings. Four are closed — the spec-tech attributions, the borrowed
observations, the missing `doc/scratchpad/`, and CLAUDE.md's absent routing. Two are no longer
findings under the narrowed scope: the unchosen stack, and no document owning the game, which the
routing table has since answered anyway.
