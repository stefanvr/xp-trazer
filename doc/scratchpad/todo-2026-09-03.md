# Open inconsistencies — 2026-09-03

**Working note. Decides nothing, and is not authority.** A snapshot from the `todo-discovery` skill.
Second run of the day, superseding the first, which is superseded rather than edited because a run is
regenerated and not maintained.

**Scope.** Process and documents only: what they claim about each other, what they point at, and
whether every kind of decision has one owner. Missing specification content and unbuilt
implementation are out of scope by definition — see the skill.

**What was examined.** All fourteen tracked files, every file reference in `doc/`,
`.claude/CLAUDE.md` and both skills tested for existence, and both guides checked for references to
project state.

---

## D-1 · "the deletion rule" is cited as though defined elsewhere, and nothing defines it

**Kind.** Dangling reference.

**Evidence.** [setup-ai-env.md:332](../setup-ai-env.md#L332) — working notes belong in
`doc/scratchpad/` *"where the deletion rule clears them when the goal lands."* No rule of that name
exists in any document. The trailing clause is the only statement of it there is.

**Done when.** Either the rule is written where it can be followed, or the phrase stops presenting
itself as a citation and simply states the rule inline.

**Note.** The directory half of this finding closed earlier: `doc/scratchpad/` now exists, and this
file is in it.

---

## What blocks what

Nothing. One finding, and it is an edit to one document.

## Examined and excluded

- **The stack is unchosen; `spec-domain`, `spec-app` and `spec-style` do not exist; nothing is
  implemented.** Out of scope by definition. CLAUDE.md's routing table states that those three are
  expected and not yet reached, so the references to them are declared rather than dangling.
- **`scripts/verify-deployment.sh`, the CI smoke test, the Firebase deployment.** Named in
  setup-ai-env.md and absent here — not findings, since those passages now attribute themselves to
  earlier projects on this machine.
- **`core.fileMode` starts noisy in a fresh clone.** Stated in SF-12 with its remedy in the
  session-start checklist.
- **`.gitignore` ends without a newline.** Cosmetic.

## Closed since the previous run

- **guide-design.md reporting that nothing owned the subject matter.** Fixed by making the sentence
  project-agnostic rather than by repointing it, since a guide may carry no project state at all. The
  document's second use of this project's domain vocabulary went with it.

## Worth noticing about the routine

Both findings of the previous run were caused by earlier repairs rather than by anything original:
one document was fixed and a sentence in another stopped being true. A document set has no build to
break, so this is the failure mode that replaces it, and the only thing that catches it is re-reading
what changed most recently. The skill now says so.
