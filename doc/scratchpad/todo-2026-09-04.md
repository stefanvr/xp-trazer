# TODO discovery — 2026-09-04

**Working note. Decides nothing, and is never cited as authority.** A snapshot of what the documents
say about each other, regenerated by re-running the `todo-discovery` skill and cleared when the goal
it belongs to lands.

Every document in `doc/`, every file in `doc/lessons/`, every `SKILL.md` and `README.md` was read or
checked. Five findings.

---

## D-1 · CLAUDE.md says `spec-app.md` does not exist

**Kind.** Stale claim.

**Evidence.** `.claude/CLAUDE.md:68` — *"`spec-app.md` does not exist yet. See **Bootstrap** above:
that means the sequence has not reached it."* `doc/spec-app.md` exists and is 70 lines.

**Done when.** The sentence goes, and with it the last claim in the routing section that any
specification is unwritten.

## D-2 · The discovery skill names both remaining specifications as unreached, and waits for a condition already met

**Kind.** Stale claim.

**Evidence.** `.claude/skills/todo-discovery/SKILL.md:16-17` — *"Never report a specification the
sequence has not reached as missing — `spec-domain.md` and `spec-app.md` today"*. Both exist.
`.claude/skills/todo-discovery/SKILL.md:64-65` — *"Once `spec-domain` and `spec-app` join
`spec-style` and `spec-tech`, checking them against each other becomes a legitimate target"* — they
have joined, and the sentence is still written as a future.

**Done when.** The prohibition names no specification, because none is now unreached, and the widen
sentence stops describing its condition as pending. *Whether the routine actually widens is the
owner's decision and is not this finding.*

## D-3 · spec-tech names a readout the code renamed

**Kind.** Stale claim.

**Evidence.** `doc/spec-tech.md:49` — *"The page's `bounces` and `vx` readouts are the proof's
instruments"*. `src/domain/simulation.ts` calls it `collisions`, `index.html` and `e2e/smoke.spec.ts`
call it `collision-count`, and the rename was made to match `doc/spec-domain.md`'s vocabulary.

**Done when.** spec-tech names the readout the code has.

## D-4 · setup-dev-env cites a lesson number that now only redirects

**Kind.** Dangling reference.

**Evidence.** `doc/setup-dev-env.md:53` — *"**Read the output, not only the exit code** — **SF-10**: a
successful `npm install` can contain a failed config load"*. `doc/setup-ai-env.md:210` is now
*"SF-10 · Moved"*, a redirect holding no lesson; the content is in `doc/lessons/node.md`.

**Done when.** setup-dev-env cites `doc/lessons/node.md`. The redirect stays for older citations
elsewhere; this is the one citation that is in a document and can be fixed.

## D-5 · Two documents carry the same table of what the deployment check distinguishes

**Kind.** Routing gap — one thing with two owners.

**Evidence.** `doc/setup-app-env.md:46-52` and `doc/lessons/github-pages.md:19-25` both carry the
three-row table of *no identifier · `unknown` · a real but different commit*. The introductions and
column headers differ; the third row is identical in both, down to *"the deployment did not fail; it
served an older build"*.

**Done when.** One of them keeps it. `guide-general`'s *Writing it down* says that when the same
explanation reaches both a pointer and its target, the pointer's copy is the one to delete — and the
routing gives `setup-app-env.md` *"how do we know it did"*, which is what the table answers.

---

## A fact about the set, not an ordering

**D-1 and D-2 have the same cause** — `spec-app` landing — and D-2's second half additionally reports
that a condition the owner set is now satisfied. Nothing blocks anything; each closes independently.

## Examined and excluded

- **`doc/lessons/` holding `firebase.md` and `sveltekit.md` for technologies this project does not
  use.** Checked specifically, because the owner asked whether the library could become a backlog. It
  has not: both files record what *went wrong* on a project that used the technology, and neither
  names anything to do. A backlog holds intentions; these hold observations.
- **CLAUDE.md's active-lessons index.** Verified against both sides: `spec-tech.md:12-19` chooses
  Node, Vite, Canvas 2D, Vitest, Playwright, GitHub Pages and GitHub Actions; `doc/lessons/` holds
  six files; the index lists exactly the four in the intersection. Correct today.
- **`spec-domain.md` and `spec-app.md` existing.** The absence of the other two specifications was
  the standing exclusion of every earlier run; it no longer applies to anything.
- **Whether the routine should widen to check specifications against each other.** D-2 reports only
  that its condition is met. Widening is a decision, and this routine reports rather than orders.
- **An artefact scan's two hits** — `/dev/style.html` in the style skill and `_probe.js` in
  setup-ai-env. The first is a URL path served in development, the second is an example of a scratch
  file that must not exist. Neither is a named artefact.
- **Overlap between `setup-app-env.md` and `doc/lessons/github-actions.md` on running checks per
  branch.** They answer different questions — what this project's pipeline does, and what goes wrong
  if a pipeline does not — and neither restates the other's words.
- **`doc/scope.md`'s In list restating things the specifications now own.** The In list is the
  scope's own content; the mechanism for held answers is its table, and all six rows are ticked.
- **O-1 in `doc/guide-override.md`.** A live override naming what would end it.
- **`grep -inE "\b(todo|fixme|tbd)\b"`** over every tracked file: no hit outside the discovery
  skill's own name and `land`'s prose about not keeping a todo list.
