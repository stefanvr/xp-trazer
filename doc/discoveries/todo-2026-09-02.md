# Open work — 2026-09-02

**What this is.** A snapshot taken by the `todo-discovery` skill, against commit `747f97a`. It decides
nothing and is not authority. Re-run the skill rather than editing this file.

**What was examined.** All eight tracked files. The repository is documentation only: no code, no
stack, no build, no tests. Every finding below is therefore one document against another, or a
document against the repository.

---

## D-1 · The stack is not chosen, and it is the goal in front of every executable one

**Kind.** Admitted gap.

**Evidence.** [spec-tech.md:8](../spec-tech.md#L8) — *"The stack is not chosen yet. No runtime,
framework, renderer or test runner has been decided, and nothing below implies one."*

**Done when.** spec-tech names the runtime, the test runner and the renderer, each proven by the
smallest thing that genuinely exercises it — *a goal never carries an unmade technology decision*
([guide-general.md:28-30](../guide-general.md#L28-L30)).

---

## D-2 · setup-ai-env.md attributes to spec-tech.md four choices spec-tech.md has not made

**Kind.** Contradiction.

**Evidence.** [setup-ai-env.md:40](../setup-ai-env.md#L40) *"Node is spec-tech's choice"* ·
[:151](../setup-ai-env.md#L151) *"The code host was spec-tech's to choose and now is GitHub"* ·
[:201](../setup-ai-env.md#L201) *"spec-tech chose GitHub Actions"* ·
[:371-372](../setup-ai-env.md#L371-L372) *"matches the version spec-tech pins — Node 24 LTS"*.
Against [spec-tech.md:8](../spec-tech.md#L8), which records no runtime, no test runner and no host.

The file anticipates exactly this and does not catch itself: [:9](../setup-ai-env.md#L9) —
*"Naming a tool here is not choosing it."* Four times it does more than name; it cites a choice by
owner, and the owner has not made it.

**Done when.** Either spec-tech records those four decisions, or setup-ai-env stops attributing them
— including the session-start table, whose *good answer* column currently reads against a pin that
does not exist.

---

## D-3 · setup-ai-env.md's observations describe a repository that is not this one

**Kind.** Dangling reference.

**Evidence.** [setup-ai-env.md:270](../setup-ai-env.md#L270) calls `scripts/verify-deployment.sh`
*"this repository's only executable file"* — there is no `scripts/` directory and `git ls-files`
returns eight files, none executable. Also [:203](../setup-ai-env.md#L203) *"the smoke test runs in
CI and fails on `unknown`"* · [:221](../setup-ai-env.md#L221) *"it is the check the Firebase
deployment is verified with"* · [:256](../setup-ai-env.md#L256) *"Observed on the first deployment
here"*. No CI workflow, build, test or deployment exists in this repository.

**Why it matters here specifically.** The document's own case for itself is
[:14-16](../setup-ai-env.md#L14-L16) — *"a rule with no observation behind it gets ignored by the
third session."* An observation whose artefact the reader cannot find fails in the same way, and
costs more, because the reader goes looking first.

**Done when.** Each observation names the project it was observed on, or the artefacts exist here.
The lessons hold either way; only the attribution is wrong.

---

## D-4 · `doc/scratchpad/` and the deletion rule that clears it are cited and absent

**Kind.** Dangling reference (two).

**Evidence.** [setup-ai-env.md:319-321](../setup-ai-env.md#L319-L321) — working notes *"belong in
`doc/scratchpad/`, in the tree and committed, where the deletion rule clears them when the goal
lands."* The directory does not exist, and *the deletion rule* is named nowhere else in the
repository.

**Done when.** The directory exists and the rule that clears it is written where it can be followed,
or the sentence points at whatever replaced it.

**Note.** This run creates `doc/discoveries/`, a sibling with the same unanswered question — what
clears it, and when. The skill answers it for discoveries only: superseded by the next run.

---

## D-5 · Two documents cite CLAUDE.md as the routing table; it routes three of the five documents

**Kind.** Contradiction.

**Evidence.** [guide-general.md:36](../guide-general.md#L36) — *"Which document owns a decision is
settled by the routing in CLAUDE.md, not by judgement in the moment."* [README.md:7-8](../../README.md#L7-L8)
— it *"routes every kind of decision to exactly one document and carries the session-start reading
order."* Against [CLAUDE.md:5,21-24](../../.claude/CLAUDE.md#L21-L24), which names setup-ai-env.md,
guide-override.md and guide-general.md. `guide-design.md` and `spec-tech.md` are not routed from it,
and no reading order appears.

**The nuance.** The routing does exist — distributed across each document's own *Owns. / Not here.*
header. So the claim is false about the file, not about the project, and there are two honest ways to
close it.

**Done when.** CLAUDE.md carries a route per decision kind and a reading order, or those two sentences
stop claiming it does.

---

## D-6 · No document owns what the game is, while the routing claims every kind is owned

**Kind.** Unowned kind.

**Evidence.** [guide-design.md:6-7](../guide-design.md#L6-L7) — *"What the game is to play is not
owned by any document yet, and does not belong here when it is."* [README.md:3](../../README.md#L3)
commits to a game: *"A C64 Traz and Arkanoid inspire game"*. [README.md:12](../../README.md#L12) and
[CLAUDE.md:21](../../.claude/CLAUDE.md#L21) both assert that each kind of decision has exactly one
owner.

**This is not a request to specify the game.** *Don't specify further than the goal in front of you
needs* rules that out, and [O-1](../guide-override.md) makes "not yet" a legitimate answer — the
routine is the product. What is open is that the answer is unstated rather than decided, so the next
session cannot tell deferral from oversight.

**Done when.** A document owns the game's rules, or one line records that nothing does yet and why.

---

## What blocks what

Recorded as fact, not as an order — the ordering is the owner's.

- **D-1 blocks anything executable.** No runtime, no test runner, no build.
- **D-2 is closable from either end**, and the cheaper end depends on D-1: if the stack is being
  chosen anyway, spec-tech records it; if not, setup-ai-env retracts the attribution today.
- **D-3, D-4, D-5 and D-6 depend on nothing.** All four are edits to documents that exist.

## Examined and excluded

- **`.gitignore` ends without a newline.** Cosmetic; no done worth naming.
- **`core.fileMode` is local configuration, so a fresh clone starts noisy.** SF-12 states it and the
  session-start checklist repairs it. Written down and handled is closed.
- **There is no test suite, CI, build or deployment.** Deliberate — the stack is not chosen (D-1),
  and the project does not build further than the goal in front of it. Their absence is not a
  finding; D-3 is about documents claiming they are here.
- **The project's own edge — what done is for trazer — is not stated in one sentence.**
  [O-1](../guide-override.md#L35) names what ends the *override*, not what finishes the project.
  Excluded as uncited: guide-general requires an edge for a goal, and no document commits trazer
  itself to being one. Worth asking the owner; not worth recording as open work.
